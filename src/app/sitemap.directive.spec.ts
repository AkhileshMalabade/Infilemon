import { SitemapDirective } from './sitemap.directive';

describe('SitemapDirective', () => {
  it('should create an instance', () => {
    const directive = new SitemapDirective();
    expect(directive).toBeTruthy();
  });
});
